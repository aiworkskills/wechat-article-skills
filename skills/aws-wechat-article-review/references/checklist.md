# 发布前检查项

审稿时按配置的必检项执行；以下为常见项，实现时与 config-schema 中的 review_required 对齐。

- **标题**：长度、禁用套路、与正文一致性。  
- **摘要**：长度、信息量、与正文一致性。  
- **正文**：敏感词、禁用词、错别字、事实与出处（按配置核查级别）、原创/转载标注。  
- **图**：封面必填、正文图与图注、链接或路径有效。  
- **链接**：外链有效性、是否需转底部引用（若实现支持）。  

自定义敏感词/禁用词以配置为准（custom_sensitive_words、forbidden_words 等）。
